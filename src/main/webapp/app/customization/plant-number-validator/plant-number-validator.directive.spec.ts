import { PlantNumberValidatorDirective } from './plant-number-validator.directive';

describe('PlantNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PlantNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
